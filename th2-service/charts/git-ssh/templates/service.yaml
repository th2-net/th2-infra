apiVersion: v1
kind: Service
metadata:
  name: {{ include "git-ssh.fullname" . }}
  labels:
    {{- include "git-ssh.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "git-ssh.selectorLabels" . | nindent 4 }}
