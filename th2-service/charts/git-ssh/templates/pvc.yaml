apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: repos-volume
  namespace: service
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-storage
  selector:
    matchLabels:
      app: repos-volume

